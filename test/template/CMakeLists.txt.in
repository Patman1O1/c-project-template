#-----------------------------------------------------------------------------------------------------------------------
# Targets
#-----------------------------------------------------------------------------------------------------------------------
list(APPEND SOURCES "${CMAKE_CURRENT_SOURCE_DIR}/${PROJECT_PRIMARY_TARGET}_test.cpp")
source_group(TREE "${CMAKE_CURRENT_SOURCE_DIR}" FILES ${SOURCES})

add_executable(${PROJECT_PRIMARY_TARGET}_test)
target_sources(${PROJECT_PRIMARY_TARGET}_test PRIVATE ${SOURCES})
target_link_libraries(${PROJECT_PRIMARY_TARGET}_test
        PRIVATE
            GTest::gtest_main
            GTest::gmock_main
)
gtest_discover_tests(${PROJECT_PRIMARY_TARGET}_test)
